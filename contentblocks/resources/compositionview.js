/* Native horizontal gallery with explicit keyboard-operable navigation; no autoplay. */
(function(){'use strict';if(window.ChisimbaCompositionViewReady)return;window.ChisimbaCompositionViewReady=true;document.querySelectorAll('.chisimba-content-block--slider').forEach(function(block){var slider=block.querySelector('.chisimba-content-slider'),controls=block.querySelector('.chisimba-slider-controls');if(!slider||!controls)return;controls.hidden=false;controls.querySelectorAll('button').forEach(function(button){button.addEventListener('click',function(){slider.scrollBy({left:Number(button.dataset.slideDirection)*slider.clientWidth,behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});});});});}());
